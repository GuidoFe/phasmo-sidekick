❓ `/help`: this message

🔦 `/item`: pick a random item

🗺️ /map LIST`: choose a random map between those indicated.If List is empty, it will consider every map. List is a sequence of these letters separated by spaces:
   - t: Tanglewood
   - e: Edgefield
   - w: Willow
   - r: Ridgeview
   - g: Grafton
   - b: Bleasdale
   - h: Brownstone Highscool
   - p: Prison
   - a: Asylum

⚔️ `\challenge`: pick a random challenge.

🌀`\spin CHALLENGE_NAME`: spin the wheel of the challenge CHALLENGE_NAME, if it has one.

🔎`clues CLUES`: Show which ghosts are possible with those clues and which evidence is lacking. Clues:
   - `emf` or `emf5`
   - `book` or `writing` or `ghostwriting`
   - `fingerprints` or `fingers`
   - `spirit` or `spiritbox`
   - `orbs` or `ghostorbs`
   - `freezing` or `temps`
   - `dots`
